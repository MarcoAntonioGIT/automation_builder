import { Card } from '@/components/global/container-scroll-animation'
import React from 'react'

type Props = {
  name: string
  description: string 
  id: string
  publish: boolean | null
}

const Workflow = ({ description, id, name, publish }: Props) => {
  return (
    
  )
}

export default Workflow